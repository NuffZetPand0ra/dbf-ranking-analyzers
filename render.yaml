services:
  - type: web
    name: dbf-ranking-analyzers
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: OPEN_PAGE
        value: dbf_dashboard.html
